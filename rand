//a function for generating an integer number between two ranges, variables(var) z> and >y
function FunctionA() {

     var x;
     x =   Math.floor((Math.random() * y) + z);
  e=document.getElementById("a").innerHTML = x;
  return e;
}
//a function for generating a decimal number between 0.0 and 1.0
function FunctionB(){
      var y,f;
     y =   Math.random();
   f=document.getElementById("b").innerHTML = y;
  return f;
}

 /*a function for generating numbers and lowercase and uppercase letters
  user choose how many
*/
function FunctionC(){
      var t="";
      var g;
      var s="0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";


    for (var i=0;i<m;i++){
      t +=s.charAt(Math.floor(Math.random()*s.length));
    }

  g=document.getElementById("c").innerHTML = t;
  return g;
}

//a function for generating a decimal number less than 1.0 or more
function FunctionD(v){
      var p,q,v;
     p =   Math.random()*v;
   q=document.getElementById("d").innerHTML = p;
  return q;
}
